# RelayBoard reference solution

This snapshot demonstrates one acceptable course result. It is not the only valid design.

Run `npm install`, `npm test`, and `npm run typecheck`. Compare behavior, tests, boundaries, and evidence—not merely line-by-line code.
