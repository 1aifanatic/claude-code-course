# RelayBoard toolkit example

This course-only plugin exposes two read-only MCP tools over fictional in-memory incidents. Review the manifest, MCP configuration, source, schemas, and removal behavior before enabling it. It uses no network, credential, or production data.
