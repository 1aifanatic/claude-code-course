---
paths:
  - "tests/**/*.ts"
---

- Use Node's built-in test runner and strict assertions.
- Name tests as observable behavior.
- For defects, demonstrate that the focused test fails against the defective baseline.
- Cover positive, negative, boundary, authorization, and concurrency behavior where relevant.
