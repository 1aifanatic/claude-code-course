# RelayBoard starter

This repository is intentionally incomplete. Its failing tests represent course work, not installation failure.

1. Run `npm install`.
2. Run `npm test` and capture the baseline.
3. Follow the course modules; do not copy the solution before attempting each lab.

The course expects you to create project instructions, skills, subagents, hooks, specifications, plans, and evidence as you progress.
